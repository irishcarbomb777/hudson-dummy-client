import React from 'react'
import styled from 'styled-components'
import {Row, Col, Form} from 'react-bootstrap'

export const DummyPlate = (plateArray) => {

  return(
    <>
      {plateArray.map(row, index)
    </>
  )
}